#include <bits/stdc++.h>
using namespace std;
#define     MAX_N   105

int main() {
    int i, j, k;
    int N, D;
    int p[MAX_N], q[MAX_N];
    cin >> N >> D;
    for (i=1; i<=N; i++) {
        cin >> p[i];
    }
    for (i=1; i<=N; i++) {
        cin >> q[i];
    }
    
    cout << count << endl;
    return 0;
}
